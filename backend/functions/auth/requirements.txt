# boto3 and cryptography are pre-installed in AWS Lambda Python 3.12 runtime.
# cryptography is a transitive dep of botocore — no pip install needed.
# No additional packages required for Cognito JWT verification.
