version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        preBuild:
          commands:
            - cd $CODEBUILD_SRC_DIR/endevo-life && npm install --legacy-peer-deps
        build:
          commands:
            - cd $CODEBUILD_SRC_DIR/endevo-life && NEXT_PUBLIC_API_URL=https://4jms6sdzk9.execute-api.us-east-1.amazonaws.com npx turbo run build --filter=@endevo/web
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - apps/web/.next/cache/**/*
      buildSettings:
        platform: WEB_COMPUTE
